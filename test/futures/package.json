{
  "name": "neon-futures-test",
  "version": "0.1.0",
  "description": "Acceptance test suite for Neon with futures feature",
  "author": "The Neon Community",
  "license": "MIT",
  "scripts": {
    "install": "cargo-cp-artifact -nc index.node -- cargo build --message-format=json-render-diagnostics",
    "test": "mocha --recursive lib"
  },
  "devDependencies": {
    "cargo-cp-artifact": "^0.1.0",
    "mocha": "^9.1.0"
  }
}
